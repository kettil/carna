{
  "hooks": {
    "commit-msg": "npx carna git:msg",
    "pre-commit": "npx carna git:commit",
    "pre-push": "echo npm run git:push",
    "post-merge": "echo npm run git:change",
    "post-rewrite": "echo npm run git:change",
    "post-checkout": "echo npm run git:change"
  }
}
